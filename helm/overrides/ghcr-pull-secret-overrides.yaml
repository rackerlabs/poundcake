# Example for private GHCR pulls.
# The installer can create/update this secret object, but the pull-secret reference itself
# must live in values or override files.

poundcakeImage:
  pullSecrets:
    - registry-creds
