# Example Helm overrides for PoundCake.
# Start by setting real FQDNs for Envoy Gateway listeners/routes.
# Copy this file to /etc/genestack/helm-configs/poundcake/poundcake-helm-overrides.yaml
# and adjust values for your environment.

# Canonical image overrides.
# Keep tags at latest by default, or pin explicit tags/digests per environment policy.
poundcakeImage:
  repository: ghcr.io/rackerlabs/poundcake
  tag: latest

uiImage:
  repository: ghcr.io/rackerlabs/poundcake-ui
  tag: latest

bakery:
  image:
    repository: ghcr.io/rackerlabs/poundcake-bakery
    tag: latest

gateway:
  enabled: true
  className: envoyproxy
  name: poundcake-gateway
  listeners:
    api:
      enabled: true
      hostname: api.example.yourdomain.com
      port: 80
      pathPrefix: /
    ui:
      enabled: true
      hostname: ui.example.yourdomain.com
      port: 80
      pathPrefix: /

# Integrated Bakery example (shared MariaDB server, separate DB/user):
# bakery:
#   database:
#     createServer: false
#     host: poundcake-mariadb
#     name: bakery
#     user:
#       name: bakery
#       # Optional external secret. If omitted, chart manages one.
#       # passwordSecret: external-bakery-db-user
#       passwordSecretKey: password
#     admin:
#       existingSecret: poundcake-secrets
#       passwordKey: DB_ROOT_PASSWORD
#     bootstrap:
#       # null -> auto-enabled when createServer=false
#       enabled: null
#       image: mariadb:11.6
#       timeoutSeconds: 300
