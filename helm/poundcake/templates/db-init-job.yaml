
{{- if or .Values.database.url .Values.database.existingSecret .Values.mariadbOperator.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "poundcake.fullname" . }}-db-init
  labels:
    {{- include "poundcake.labels" . | nindent 4 }}
    app.kubernetes.io/component: db-init
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "1"
    "helm.sh/hook-delete-policy": before-hook-creation
spec:
  backoffLimit: 3
  template:
    metadata:
      name: {{ include "poundcake.fullname" . }}-db-init
      labels:
        {{- include "poundcake.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: db-init
    spec:
      restartPolicy: OnFailure
      serviceAccountName: {{ include "poundcake.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: db-init
        securityContext:
          {{- toYaml .Values.securityContext | nindent 10 }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        command:
        - /bin/bash
        - -c
        - |
          set -e

          echo "========================================"
          echo "PoundCake Database Migration"
          echo "========================================"

          {{- if .Values.mariadbOperator.enabled }}
          # Construct database URL with password from secret
          export POUNDCAKE_DATABASE_URL="mysql+pymysql://{{ include "poundcake.mariadbUsername" . }}:${MARIADB_PASSWORD}@{{ include "poundcake.mariadbHost" . }}:3306/{{ include "poundcake.mariadbDatabaseName" . }}"
          {{- end }}

          # Change to project root where alembic.ini lives
          cd /app

          # Debug: Check if alembic.ini exists and is readable
          echo "Checking alembic.ini..."
          if [ -f /app/alembic.ini ]; then
            echo "✓ alembic.ini exists"
            echo "First 20 lines of alembic.ini:"
            head -20 /app/alembic.ini
          else
            echo "✗ ERROR: alembic.ini not found!"
            exit 1
          fi

          # Wait for database to be ready
          echo "Waiting for database connection..."
          MAX_RETRIES=30
          RETRY_COUNT=0

          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            if python3 -c "from api.core.database import engine; engine.connect()" 2>/dev/null; then
              echo "✓ Database connection successful"
              break
            fi
            RETRY_COUNT=$((RETRY_COUNT + 1))
            echo "Attempt ${RETRY_COUNT}/${MAX_RETRIES}: Waiting for database..."
            sleep 2
          done

          if [ $RETRY_COUNT -ge $MAX_RETRIES ]; then
            echo "✗ ERROR: Database did not become ready after ${MAX_RETRIES} attempts"
            exit 1
          fi

          # Check if any migrations exist (excluding __init__.py and __pycache__)
          MIGRATION_COUNT=$(find alembic/versions -name "*.py" ! -name "__init__*" ! -path "*/__pycache__/*" 2>/dev/null | wc -l)

          if [ "$MIGRATION_COUNT" -eq 0 ]; then
            echo "[WARN] No migrations found. Creating initial schema migration..."
            alembic -c /app/alembic.ini revision --autogenerate -m "initial_schema"
            echo "[OK] Initial migration created"
          else
            echo "[OK] Found $MIGRATION_COUNT existing migration(s)"
          fi

          # Run database migrations
          echo "Running database migrations..."
          alembic -c /app/alembic.ini upgrade head

          if [ $? -eq 0 ]; then
            echo "✓ Database migrations applied successfully"
          else
            echo "✗ Database migration failed!"
            exit 1
          fi

          # Show current revision
          echo ""
          echo "Current database revision:"
          alembic -c /app/alembic.ini current

          # Seed default data
          echo ""
          echo "Seeding default data..."
          python3 /app/api/scripts/init_database.py

          if [ $? -eq 0 ]; then
            echo "✓ Default data seeded successfully"
          else
            echo "⚠ Default data seeding failed (non-fatal)"
          fi

          echo "========================================"
          echo "Migration Complete"
          echo "========================================"
        env:
          {{- if or .Values.database.url .Values.database.existingSecret }}
          - name: POUNDCAKE_DATABASE_URL
            {{- if .Values.database.existingSecret }}
            valueFrom:
              secretKeyRef:
                name: {{ .Values.database.existingSecret }}
                key: {{ .Values.database.secretKey }}
            {{- else }}
            value: {{ .Values.database.url | quote }}
            {{- end }}
          {{- else if .Values.mariadbOperator.enabled }}
          - name: MARIADB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ include "poundcake.mariadbUserSecretName" . }}
                key: password
          {{- end }}
          - name: POUNDCAKE_LOG_LEVEL
            value: "INFO"
        {{- with .Values.resources }}
        resources:
          {{- toYaml . | nindent 10 }}
        {{- end }}
{{- end }}
