{{- if .Values.mariadbOperator.enabled }}
{{- if .Capabilities.APIVersions.Has "k8s.mariadb.com/v1alpha1" }}
apiVersion: k8s.mariadb.com/v1alpha1
kind: Database
metadata:
  name: {{ include "poundcake.fullname" . }}-db
  namespace: {{ include "poundcake.mariadbNamespace" . }}
  labels:
    {{- include "poundcake.labels" . | nindent 4 }}
    app.kubernetes.io/component: mariadb
spec:
  # Reference to the MariaDB instance
  mariaDbRef:
    name: {{ include "poundcake.mariadbName" . }}
    waitForIt: true

  # Database name
  name: {{ include "poundcake.mariadbDatabaseName" . }}

  # Character set and collation
  characterSet: utf8mb4
  collate: utf8mb4_unicode_ci

  # Retry configuration for database creation
  retryInterval: 5s
{{- end }}
{{- end }}
