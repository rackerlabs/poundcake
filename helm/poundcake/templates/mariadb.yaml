# ____                        _  ____      _
# |  _ \ ___  _   _ _ __   __| |/ ___|__ _| | _____
# | |_) / _ \| | | | '_ \ / _` | |   / _` | |/ / _ \
# |  __/ (_) | |_| | | | | (_| | |__| (_| |   <  __/
# |_|   \___/ \__,_|_| |_|\__,_|\____\__,_|_|\_\___|
#
# ╔════════════════════════════════════════════════════════════════╗
# ____                        _  ____      _         
# |  _ \ ___  _   _ _ __   __| |/ ___|__ _| | _____ 
# | |_) / _ \| | | | '_ \ / _` | |   / _` | |/ / _ \
# |  __/ (_) | |_| | | | | (_| | |__| (_| |   <  __/
# |_|   \___/ \__,_|_| |_|\__,_|\____\__,_|_|\_\___|
# ╚════════════════════════════════════════════════════════════════╝

{{- if .Values.mariadbOperator.enabled }}
{{- if .Capabilities.APIVersions.Has "k8s.mariadb.com/v1alpha1" }}
apiVersion: k8s.mariadb.com/v1alpha1
kind: MariaDB
metadata:
  name: {{ include "poundcake.mariadbName" . }}
  namespace: {{ include "poundcake.mariadbNamespace" . }}
  labels:
    {{- include "poundcake.labels" . | nindent 4 }}
    app.kubernetes.io/component: mariadb
spec:
  rootPasswordSecretKeyRef:
    name: {{ include "poundcake.mariadbRootSecretName" . }}
    key: {{ .Values.mariadbOperator.server.rootPasswordSecretKey | default "password" }}

  image: "{{ .Values.mariadbOperator.server.image.repository }}:{{ .Values.mariadbOperator.server.image.tag }}"
  imagePullPolicy: {{ .Values.mariadbOperator.server.image.pullPolicy }}

  port: 3306

  {{- if gt (int .Values.mariadbOperator.server.replicas) 1 }}
  replicas: {{ .Values.mariadbOperator.server.replicas }}
  galera:
    enabled: true
    primary:
      podIndex: 0
      automaticFailover: true
    sst: mariabackup
    replicaThreads: 1
  {{- else }}
  replicas: 1
  {{- end }}

  storage:
    size: {{ .Values.mariadbOperator.server.storage.size }}
    {{- if .Values.mariadbOperator.server.storage.storageClassName }}
    storageClassName: {{ .Values.mariadbOperator.server.storage.storageClassName }}
    {{- end }}

  {{- with .Values.mariadbOperator.server.resources }}
  resources:
    {{- toYaml . | nindent 4 }}
  {{- end }}

  # Connection settings
  myCnf: |
    [mariadb]
    bind-address=*
    default_storage_engine=InnoDB
    binlog_format=row
    innodb_autoinc_lock_mode=2
    max_allowed_packet=256M
    max_connections=200

  # Health checks
  livenessProbe:
    initialDelaySeconds: 20
    periodSeconds: 5
    timeoutSeconds: 5

  readinessProbe:
    initialDelaySeconds: 20
    periodSeconds: 5
    timeoutSeconds: 5

  # Service configuration
  service:
    type: ClusterIP

  # Update strategy
  updateStrategy:
    type: ReplicasFirstPrimaryLast

{{- else }}
# MariaDB Operator CRDs not found in cluster
# Install the mariadb-operator first:
#   helm repo add mariadb-operator https://mariadb-operator.github.io/mariadb-operator
#   helm install mariadb-operator mariadb-operator/mariadb-operator
{{- fail "MariaDB Operator CRDs (k8s.mariadb.com/v1alpha1) not found. Install mariadb-operator first or set mariadbOperator.enabled=false" }}
{{- end }}
{{- end }}
