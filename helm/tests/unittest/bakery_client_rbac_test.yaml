suite: bakery-client-rbac
templates:
  - templates/bakery-client-rbac.yaml
tests:
  - it: renders read-only collector rbac in the monitor namespace when bakery client is enabled
    set:
      poundcake.enabled: true
      bakery.client.enabled: true
      bakery.client.baseUrl: https://bakery.example.com
      bakery.client.monitor.namespace: rackspace
      serviceAccount.create: true
      serviceAccount.name: ""
    asserts:
      - hasDocuments:
          count: 4
      - equal:
          path: kind
          value: ClusterRole
        documentIndex: 0
      - equal:
          path: metadata.name
          value: RELEASE-NAME-poundcake-bakery-collectors-cluster
        documentIndex: 0
      - equal:
          path: rules[0].resources[0]
          value: nodes
        documentIndex: 0
      - equal:
          path: kind
          value: ClusterRoleBinding
        documentIndex: 1
      - equal:
          path: subjects[0].namespace
          value: NAMESPACE
        documentIndex: 1
      - equal:
          path: kind
          value: Role
        documentIndex: 2
      - equal:
          path: metadata.name
          value: RELEASE-NAME-poundcake-bakery-collectors
        documentIndex: 2
      - equal:
          path: metadata.namespace
          value: rackspace
        documentIndex: 2
      - equal:
          path: rules[0].resources[0]
          value: pods
        documentIndex: 2
      - equal:
          path: rules[0].resources[2]
          value: persistentvolumeclaims
        documentIndex: 2
      - equal:
          path: rules[1].resources[1]
          value: statefulsets
        documentIndex: 2
      - equal:
          path: kind
          value: RoleBinding
        documentIndex: 3
      - equal:
          path: subjects[0].name
          value: RELEASE-NAME-poundcake
        documentIndex: 3
      - equal:
          path: subjects[0].namespace
          value: NAMESPACE
        documentIndex: 3

  - it: includes extra namespaces once each
    values:
      - fixtures/bakery_client_rbac_extra_namespaces.yaml
    release:
      namespace: rackspace
    asserts:
      - hasDocuments:
          count: 6
      - equal:
          path: kind
          value: ClusterRole
        documentIndex: 0
      - equal:
          path: kind
          value: ClusterRoleBinding
        documentIndex: 1
      - equal:
          path: metadata.namespace
          value: rackspace
        documentIndex: 2
      - equal:
          path: metadata.namespace
          value: rackspace
        documentIndex: 3
      - equal:
          path: metadata.namespace
          value: observability
        documentIndex: 4
      - equal:
          path: metadata.namespace
          value: observability
        documentIndex: 5

  - it: does not render collector rbac when disabled
    set:
      poundcake.enabled: true
      bakery.client.enabled: true
      bakery.client.baseUrl: https://bakery.example.com
      bakery.client.collectionRbac.enabled: false
    asserts:
      - hasDocuments:
          count: 0
